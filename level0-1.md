## Bandit Level 0 → Level 1

### Goal
Find the password for the next level.

### Approach
The password is stored in a file named `readme` in the home directory.

### Solution
```bash
cat readme
```

### What I learned
- Basic file reading in Linux using `cat`