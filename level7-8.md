## Bandit Level 7 → Level 8

### Goal
Find a specific word inside a text file.

### Approach
Use `grep` with exact word matching.

### Solution
```bash
grep -w "millionth" data.txt
```

### What I learned
- Searching text with `grep`
- Importance of word boundaries